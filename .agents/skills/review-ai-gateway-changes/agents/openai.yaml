interface:
  display_name: "AI Gateway Reviewer"
  short_description: "Review gateway diffs for production failure modes"
  default_prompt: "Use $review-ai-gateway-changes to independently review this change and report actionable findings."
