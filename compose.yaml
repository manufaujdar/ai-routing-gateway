services:
  gateway:
    build: .
    ports:
      - "127.0.0.1:8000:8000"
    env_file:
      - path: .env
        required: false
    environment:
      AI_GATEWAY_ALLOW_RUNTIME_CREDENTIALS: "false"
    restart: unless-stopped
